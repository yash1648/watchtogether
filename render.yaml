services:
  - type: web
    name: watchtogether-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        value: https://watchtogether-jet.vercel.app
      - key: NODE_ENV
        value: production
